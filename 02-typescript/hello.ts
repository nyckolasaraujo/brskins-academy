const nome: string = "BRSKINS";
const ano: number = 2026;

console.log("Olá, " + nome + "! Estamos no ano de " + ano + ".");

for (let i = 1; i <= 3; i++) {
    if (i % 2 === 0) {
        console.log(i + " é par");
    }
    else {
        console.log(i + " é impar");

    }
}